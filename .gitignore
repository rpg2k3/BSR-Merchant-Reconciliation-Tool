__pycache__/
*.pyc
*.pyo
.env
*.egg-info/

# Build output
dist/
build/
*.spec.bak
*.manifest

# Config with API keys
.bsr_recon_config.json
# Real secrets (Anthropic API key etc.) — only secrets.yaml.example is tracked.
config/secrets.yaml

# Real statement/ledger sample data — never goes to GitHub (Phase 5 repo
# hygiene). At-rest protection is VeraCrypt full-disk encryption on the
# external SSD; the samples live there for local dev/testing only.
samples/

# Keep these files
!assets/icon.png
!build.sh
!install.sh
!uninstall.sh
!BSR_Recon.spec
